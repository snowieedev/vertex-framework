import type { VertexConfig } from './types.js';

/**
 * Helper to define VERTEX configuration with type checking.
 */
export function defineConfig(config: VertexConfig): VertexConfig {
  return config;
}
