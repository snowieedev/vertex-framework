export interface VertexConfig {
  /**
   * Application base path
   */
  base?: string;
  /**
   * Dev server port
   */
  port?: number;
  /**
   * Enable/disable strict mode
   */
  strict?: boolean;
}
